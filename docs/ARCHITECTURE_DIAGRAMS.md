# Enhanced Buffering System: Architecture Diagrams

## System Overview

```
┌────────────────────────────────────────────────────────────────────────┐
│                         REALTIME VISION SYSTEM                         │
│                  WITH ENHANCED BUFFERING & AUTO-RECOVERY               │
└────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────┐
│      EDGE DEVICE (Factory)       │
│                                  │
│  ┌────────────────────────────┐  │
│  │  YOLO Inference            │  │
│  │  (GPU/CPU)                 │  │
│  │  Processing time: ~45ms    │  │
│  └──────────┬─────────────────┘  │
│             │                     │
│             ▼                     │
│  ┌────────────────────────────┐  │
│  │  EnhancedAsyncLogging      │  │
│  │  Client                    │  │
│  │  ┌──────────────────────┐  │  │
│  │  │ In-Memory Batch (32) │  │  │
│  │  │ + Timeout (10s)      │  │  │
│  │  └──────────────────────┘  │  │
│  │  ┌──────────────────────┐  │  │
│  │  │ Connectivity Monitor │  │  │
│  │  │ (every 30s)          │  │  │
│  │  └──────────────────────┘  │  │
│  │  ┌──────────────────────┐  │  │
│  │  │ Recovery Manager     │  │  │
│  │  │ (auto-retry)         │  │  │
│  │  └──────────────────────┘  │  │
│  └──────────┬─────────────────┘  │
│             │                     │
│   ┌─────────┴─────────┐           │
│   ▼                   ▼           │
│  Cloud API         Disk Storage   │
│  (if available)   (always)        │
│                   /data/          │
│                prediction_buffer  │
│                .db                │
│                                  │
└──────────────────────────────────┘
         │                         ▲
         │ [Batch over HTTP]       │ [Load on startup]
         │ [Retry on failure]      │
         ▼                         │
┌──────────────────────────────────┴───────┐
│     CLOUD LOGGING SERVICE (FastAPI)      │
│                                          │
│  ┌────────────────────────────────────┐  │
│  │ POST /log (receive batches)        │  │
│  │ GET /stats (query statistics)      │  │
│  │ GET /health (liveness check)       │  │
│  └────────────────────────────────────┘  │
│                ▼                         │
│  ┌────────────────────────────────────┐  │
│  │ SQLite Database (predictions)      │  │
│  │ ├─ Timestamp                       │  │
│  │ ├─ Model version                   │  │
│  │ ├─ Detections                      │  │
│  │ └─ Device ID                       │  │
│  └────────────────────────────────────┘  │
│                                          │
└──────────────────────────────────────────┘
```

## Prediction Flow: Cloud Available

```
    PREDICTION LOGGED
          │
          ▼
    ┌─────────────────┐
    │ In-Memory Batch │
    └────────┬────────┘
             │
             ├─ Add to queue (32 max)
             │
             ▼
    ┌─────────────────┐
    │ Batch Ready?    │
    │ (32 OR 10s)     │
    └────────┬────────┘
             │
             YES (timeout)
             │
             ▼
    ┌─────────────────┐
    │ Check Cloud     │
    │ Connection      │
    └────────┬────────┘
             │
             OK
             │
             ▼
    ┌─────────────────┐
    │ Send to Cloud   │
    │ via HTTP POST   │
    │ Timeout: 30s    │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │ Response?       │
    │ Status 200      │
    └────────┬────────┘
             │
             YES
             │
             ▼
    ┌─────────────────┐
    │ Mark as "sent"  │
    │ in SQLite       │
    └────────┬────────┘
             │
             ▼
       ✅ SUCCESS
       (Latency: 50-200ms)
```

## Prediction Flow: Cloud Unavailable

```
    PREDICTION LOGGED
          │
          ▼
    ┌─────────────────┐
    │ In-Memory Batch │
    └────────┬────────┘
             │
             ├─ Add to queue
             │
             ▼
    ┌─────────────────┐
    │ Persist to      │
    │ SQLite          │
    │ (ALWAYS)        │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │ Batch Ready?    │
    └────────┬────────┘
             │
             YES
             │
             ▼
    ┌─────────────────┐
    │ Check Cloud     │
    │ Connection      │
    └────────┬────────┘
             │
             FAILED
             │
             ▼
    ┌─────────────────┐
    │ Retry with      │
    │ Exponential     │
    │ Backoff         │
    │ (2^n seconds)   │
    └────────┬────────┘
             │
             Max retries
             exceeded
             │
             ▼
    ┌─────────────────┐
    │ Mark as         │
    │ "pending" in    │
    │ SQLite          │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │ Continue        │
    │ Buffering       │
    │ Locally         │
    └────────┬────────┘
             │
             ▼
       💾 SAFE (on disk)
       (Survives crash)
       (Waits for recovery)
```

## Recovery Flow: Connectivity Restored

```
    CONNECTIVITY MONITOR
    (Background task)
          │
          │ Every 30 seconds
          ▼
    ┌─────────────────┐
    │ HEAD /health    │
    │ to Cloud API    │
    │ (5s timeout)    │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │ Status OK?      │
    └────────┬────────┘
             │
             YES (restored)
             │
             ▼
    ┌─────────────────┐
    │ Trigger         │
    │ Recovery        │
    │ Manager         │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │ Query SQLite    │
    │ for pending     │
    │ predictions     │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │ Batch load      │
    │ (batch_size=32) │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │ Send to Cloud   │
    │ HTTP POST       │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │ Success?        │
    └────────┬────────┘
             │
             YES
             │
             ▼
    ┌─────────────────┐
    │ Mark as "sent"  │
    │ in SQLite       │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │ Continue        │
    │ recovery loop   │
    │ until all sent  │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │ Auto-cleanup    │
    │ sent >7 days    │
    └────────┬────────┘
             │
             ▼
       ✅ RECOVERY COMPLETE
       (Zero data loss)
```

## Component Interaction Diagram

```
┌─────────────────────────────────────────────────────────────┐
│ ENHANCED ASYNC LOGGING CLIENT                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────┐   ┌──────────────────┐              │
│  │ log_prediction() │──►│ In-Memory Queue  │              │
│  │ (user entry)     │   │ (batch_size=32)  │              │
│  └──────────────────┘   └────────┬─────────┘              │
│                                  │                         │
│                                  ▼                         │
│  ┌──────────────────┐   ┌──────────────────┐              │
│  │LocalBufferManager│◄──│ add_prediction() │              │
│  │ (SQLite DB)      │   │ (async persist)  │              │
│  └────────┬─────────┘   └──────────────────┘              │
│           │                                                │
│  ┌────────┴─────────────────┐                             │
│  ▼                          ▼                              │
│  get_batch()   ┌──────────────────────┐                   │
│  mark_sent()   │ BufferRecoveryManager│                   │
│  mark_failed() │ - start_recovery()   │                   │
│  cleanup()     │ - auto-retry loop    │                   │
│  check_space() │ - batch send         │                   │
│                └──────────────────────┘                   │
│                          │                                 │
│                          ▼                                 │
│  ┌──────────────────────────────────────┐                │
│  │ Connectivity Monitor (every 30s)    │                │
│  │ - HEAD /health to API                │                │
│  │ - Detects up/down/restored           │                │
│  │ - Triggers recovery on change        │                │
│  └──────────────────────────────────────┘                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
         │                              │
         │ send_batch()                 │ get_buffer_status()
         │ (on batch ready)             │ (monitor.get_full_status())
         │                              │
         ▼                              ▼
   ┌──────────────┐          ┌──────────────────┐
   │ Cloud API    │          │ BufferMonitor    │
   │ /log         │          │ - get_full_status│
   │ (HTTP POST)  │          │ - print_status   │
   └──────────────┘          │ - export_stats   │
                             │ - repair_db      │
                             └──────────────────┘
```

## Database Schema

```
┌──────────────────────────────────────────────────┐
│ PREDICTION_BUFFER TABLE                          │
├──────────────────────────────────────────────────┤
│                                                  │
│ id (INT, PK)                                     │
│ ├─ Auto-increment unique identifier             │
│ │                                               │
│ prediction_data (TEXT)                          │
│ ├─ JSON: {detections, confidence, etc}         │
│ │                                               │
│ image_id (TEXT)                                 │
│ ├─ Reference to original image                  │
│ │                                               │
│ timestamp (REAL)                                │
│ ├─ Prediction timestamp (query, ordering)      │
│ │                                               │
│ buffered_at (REAL)                              │
│ ├─ When buffered to disk                        │
│ │                                               │
│ sent_at (REAL, NULLABLE)                        │
│ ├─ When sent to cloud (NULL if pending)        │
│ │                                               │
│ status (TEXT)                                   │
│ ├─ 'pending' | 'sent' | 'failed'               │
│ │                                               │
│ retry_count (INT)                               │
│ ├─ Number of send attempts                      │
│ │                                               │
│ last_error (TEXT)                               │
│ ├─ Last error message (for debugging)           │
│ │                                               │
├──────────────────────────────────────────────────┤
│ INDEXES:                                         │
│ ├─ idx_status (status)                          │
│ ├─ idx_image_id (image_id)                      │
│ ├─ idx_timestamp (timestamp DESC)               │
│ └─ idx_buffered_at (buffered_at DESC)          │
│                                                  │
├──────────────────────────────────────────────────┤
│ BUFFER_METADATA TABLE                           │
├──────────────────────────────────────────────────┤
│ total_buffered (INT)                            │
│ total_sent (INT)                                │
│ total_failed (INT)                              │
│ last_cleanup (REAL)                             │
│                                                  │
└──────────────────────────────────────────────────┘
```

## Deployment Architecture: Kubernetes

```
┌───────────────────────────────────────────────────────────┐
│ KUBERNETES CLUSTER                                        │
├───────────────────────────────────────────────────────────┤
│                                                           │
│  ┌──────────────────────────────────────────────────┐   │
│  │ StatefulSet: edge-inference                     │   │
│  │ Replicas: 1                                     │   │
│  ├──────────────────────────────────────────────────┤   │
│  │                                                 │   │
│  │  Pod 0:                                        │   │
│  │  ┌────────────────────────────────────────┐   │   │
│  │  │ Container: inference                   │   │   │
│  │  │ Image: edge-inference:latest           │   │   │
│  │  │ Port: 8000                             │   │   │
│  │  │ Env:                                   │   │   │
│  │  │  - BUFFER_DB_PATH=/data/buffer.db     │   │   │
│  │  │  - CLOUD_LOGGING_ENDPOINT=...         │   │   │
│  │  │ Mounts:                                │   │   │
│  │  │  - buffer-storage:/data                │   │   │
│  │  │ Probes:                                │   │   │
│  │  │  - liveness: GET /health               │   │   │
│  │  │  - readiness: GET /ready               │   │   │
│  │  └────────────────────────────────────────┘   │   │
│  │  ▼                                             │   │
│  │  ┌────────────────────────────────────────┐   │   │
│  │  │ PersistentVolume (buffer-storage)      │   │   │
│  │  │ Type: ReadWriteOnce                    │   │   │
│  │  │ Size: 1Gi                              │   │   │
│  │  │ Path: /data/prediction_buffer.db       │   │   │
│  │  └────────────────────────────────────────┘   │   │
│  │                                                 │   │
│  └──────────────────────────────────────────────────┘   │
│          │                                               │
│          ├──► Service: edge-inference (ClusterIP)      │
│          │    Port: 8000                               │
│          │                                              │
│          ▼                                              │
│  ┌──────────────────────────────────────────────────┐   │
│  │ Deployment: logging-service                     │   │
│  │ Replicas: 2                                     │   │
│  ├──────────────────────────────────────────────────┤   │
│  │                                                 │   │
│  │  Pod 0:                    Pod 1:              │   │
│  │  ┌──────────────────┐     ┌──────────────────┐│   │
│  │  │Container: api    │     │Container: api    ││   │
│  │  │Port: 8001        │     │Port: 8001        ││   │
│  │  └──────────────────┘     └──────────────────┘│   │
│  │         │                         │             │   │
│  │         └────────┬────────────────┘             │   │
│  │                  ▼                              │   │
│  │         Shared Storage (CloudSQL)               │   │
│  │         predictions_db                         │   │
│  │                                                 │   │
│  └──────────────────────────────────────────────────┘   │
│          │                                               │
│          ├──► Service: logging-service (LoadBalancer)  │
│          │    Port: 8001                               │
│          │    External IP: available                   │
│          │                                              │
│                                                        │
└───────────────────────────────────────────────────────────┘
         │
         │ HTTP POST /log
         │ (batch of predictions)
         │
         ▼
    [Cloud Logging API]
```

## Docker Compose Architecture

```
┌──────────────────────────────────────────────┐
│ DOCKER COMPOSE (docker-compose.yml)          │
├──────────────────────────────────────────────┤
│                                              │
│  ┌────────────────────────────────────────┐ │
│  │ Service: edge-inference                │ │
│  │ Build: . (current directory)           │ │
│  │ Ports: 8000:8000                       │ │
│  │ Environment:                           │ │
│  │  - BUFFER_DB_PATH=/data/buffer.db     │ │
│  │  - CLOUD_LOGGING_ENDPOINT=...         │ │
│  │ Volumes:                               │ │
│  │  - edge-buffer:/data                   │ │
│  │ Depends on: logging-service            │ │
│  │ Restart: unless-stopped                │ │
│  └────────────────────────────────────────┘ │
│         │                                     │
│         ▼                                     │
│  ┌────────────────────────────────────────┐ │
│  │ Named Volume: edge-buffer              │ │
│  │ Driver: local                          │ │
│  │ Path: /var/lib/docker/volumes/...     │ │
│  │ Contents:                              │ │
│  │  - prediction_buffer.db (SQLite)      │ │
│  └────────────────────────────────────────┘ │
│                                              │
│  ┌────────────────────────────────────────┐ │
│  │ Service: logging-service               │ │
│  │ Build: ./src/cloud_logging             │ │
│  │ Ports: 8001:8001                       │ │
│  │ Volumes:                               │ │
│  │  - logging-data:/data                  │ │
│  │ Restart: unless-stopped                │ │
│  └────────────────────────────────────────┘ │
│         │                                     │
│         ▼                                     │
│  ┌────────────────────────────────────────┐ │
│  │ Named Volume: logging-data             │ │
│  │ Driver: local                          │ │
│  │ Contents:                              │ │
│  │  - predictions.db (SQLite)             │ │
│  └────────────────────────────────────────┘ │
│                                              │
│  Network: vision-network (bridge)            │
│                                              │
└──────────────────────────────────────────────┘
```

## State Transitions

```
PREDICTION LIFECYCLE:

┌─────────────────────────────────────────────────────────┐
│ PENDING STATE (buffered, awaiting send)                 │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Triggered by:                                         │
│  - Cloud unavailable                                   │
│  - Connection timeout                                  │
│  - Server error (5xx)                                  │
│  - Backoff retry in progress                           │
│                                                         │
│  Actions:                                              │
│  - Store in SQLite                                     │
│  - Increment retry_count                              │
│  - Schedule exponential backoff retry                 │
│  - Update last_error message                          │
│                                                         │
└─────────────────────────────────────────────────────────┘
         │
         │ [Cloud becomes available]
         │ [Recovery triggered]
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│ SENT STATE (successfully delivered to cloud)            │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Triggered by:                                         │
│  - HTTP 200 response from /log endpoint               │
│  - Cloud API confirms receipt                         │
│                                                         │
│  Actions:                                              │
│  - Update sent_at timestamp                           │
│  - Update status = 'sent'                             │
│  - Mark for eventual cleanup                          │
│  - Update buffer stats                                │
│                                                         │
└─────────────────────────────────────────────────────────┘
         │
         │ [After cleanup_after_days (default: 7)]
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│ DELETED STATE (cleaned from database)                   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Cleanup policy:                                       │
│  - Automatically delete sent predictions older than    │
│    cleanup_after_days (default: 7 days)              │
│  - Triggered automatically when:                      │
│    * Buffer reaches 90% capacity                      │
│    * Manual cleanup() called                          │
│    * Scheduled daily cleanup                          │
│                                                         │
│  Purpose:                                              │
│  - Prevent unbounded database growth                  │
│  - Reclaim disk space                                 │
│  - Maintain performance (VACUUM operation)            │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

**Last Updated**: January 28, 2025  
**Status**: Production Ready ✅
